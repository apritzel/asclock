Things to be implemented:

  - put all language specific stuff into ascii and assemble it from an
    alphabet pixmap.

  - analog clock support for the xlib version (this one).

  - make configure more script-friendly (matching autotools)
  - add more files to make install target
